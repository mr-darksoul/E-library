<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="8dp">

    <ImageView
        android:id="@+id/bookThumbImage"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:src="@mipmap/ic_launcher"
        />

    <TextView
        android:id="@+id/nameTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="Book Name"
        android:paddingLeft="4dp"
        android:layout_toRightOf="@id/bookThumbImage"
        android:layout_toEndOf="@id/bookThumbImage"
        android:textSize="24sp"/>
    <TextView
        android:id="@+id/authorTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="4dp"
        android:layout_below="@id/nameTextView"
        android:paddingTop="12dp"
        android:textSize="18sp"
        tools:text="Author"
        android:layout_toRightOf="@id/bookThumbImage"
        android:layout_toEndOf="@id/bookThumbImage"/>
    <TextView
        android:id="@+id/priceTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Price:900"
        android:layout_alignBaseline="@+id/authorTextView"
        android:layout_alignBottom="@+id/authorTextView"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

</RelativeLayout>